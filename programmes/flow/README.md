# programmes/flow - **SCOPED, NOT OPEN**

Family 3 of `ROADMAP.md`: mechanical flow - merger arbitrage and index-reconstitution flow.

**This programme is not open.** No broker account, no deal database, no screen, no capital. The gate
- *held until another family is producing* - is unchanged, and nothing is producing.

## The verdict

| strand | verdict |
|---|---|
| **S1** merger arbitrage | **could not establish** |
| **S2** index-reconstitution flow | **could not establish**, deletion leg dead on a measured figure |

**Family 3 does not survive scoping. Neither strand is killed on its arithmetic, because in neither
case was the arithmetic evaluable** - which is itself the finding.

**Read the verdict with its caution attached:** every unestablished item is on the cost, loss or
observability side, and the one measured return figure in the packet is a decay to approximately
zero. The verdict is agnostic; the evidence is not.

## The documents, in the order they were written

| file | what it is |
|---|---|
| `KILL-CONDITION.md` | **T1.** Sealed alone before any source was read. Two loss thresholds, a breadth floor named before any spread figure, six of twelve parameters derived rather than assumed. **Not to be edited** |
| `PRIORS.md` | **T2.** Both strands: mechanism, the item-5 answer, magnitudes, and section 6.1 on every one |
| `MECHANICS.md` | **T3.** The HSR rule and the Russell methodology, from primary documents. Silences named |
| `BREAKS.md` | **T4.** Break rate, severity and clustering; unretrieved and unknowable kept apart |
| `SCOPING-VERDICT.md` | **T5.** The comparison against T1, quoted verbatim |
| `registry/F3-FLOW-SCOPING.md` | Registry entry `F3-01`, `revive_if`, and what is **not** a revive condition |
| `CLOSEOUT-PACKET9.md` | MEASURED / INFERRED / ASSUMED; the item-5 answer; two recorded T1 defects |
| `PROGRESS.md` | Task-by-task log, so a restarted session resumes from files |

## The two findings worth carrying

> **Clustering is documented by three independent mechanisms and quantified by none.** The
> short-put payoff shape, the 2008 financing withdrawal, and a common-cause MAE trigger in 2020.
> **None of the three is antitrust** - the one channel the regulatory record covers.

> **Required breadth never falls below roughly fifty simultaneous positions** anywhere in the
> strategy's positive zone, and is **57 at T1's own break-rate ceiling.** That is a structural
> consequence of a capped gain against a loss eight times its size, computed from formulas sealed
> before any evidence was read.
